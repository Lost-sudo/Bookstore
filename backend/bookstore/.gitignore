.env                      # Contains sensitive environment variables (SECRET_KEY, DB credentials, API keys)
backend/env/
.python-version           # For pyenv users

*.pyc                     # Compiled Python bytecode files
__pycache__/              # Python 3 bytecode cache directories
*.log                     # Log files (e.g., Django's development server logs)
*.pot                     # Portable Object Template files (for translations)
db.sqlite3                # SQLite database file (unless you explicitly want to track a small, initial dev DB)

media/                    # User-uploaded media files (crucial to ignore as they are typically not source code)
staticfiles/              # Collected static files (generated by `collectstatic` on deploy)


.Python
build/
develop-eggs/
dist/
eggs/
.egg-info/
*.egg
*.whl
*.zip
*.tar.gz

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Coverage reports
.coverage
.coverage.*
htmlcov/

# For testing
.pytest_cache/
.tox/